# Installed here rather than at the workspace root only because this worktree shares the root's
# node_modules with another checkout. A normal clone installs from the workspace and needs none of it.
node_modules/
dist/
